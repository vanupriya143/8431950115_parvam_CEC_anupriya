#include <stdio.h>
int main()
{ 
    int size=0
    if even(size%2==0)
    {
    printf("  the sum of even numbers");
    scanf("%d",&size);
    }
    else odd(i%d==!0)
printf("the sum of odd numbers");
scanf("%d",&size);
 
return 0;
}